% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extend_time_series.R
\name{extend_time_series}
\alias{extend_time_series}
\title{Extend a Time Series into the Future}
\usage{
extend_time_series(
  data,
  startdate,
  period = c("year", "semester", "quarter", "month"),
  length,
  round_dates = c("ceiling", "floor", "none"),
  round_unit = NULL,
  fillyears = 3,
  minyear = NULL,
  maxyear = NULL,
  present_vars = c("stored", "detect"),
  response_var
)
}
\arguments{
\item{data}{A data frame containing historical records.  Must include a
\code{date} column of class \code{Date} and numeric predictor columns.}

\item{startdate}{A scalar coercible to \code{Date} via \code{\link[lubridate:ymd]{lubridate::ymd()}}.  The
first future date to generate.}

\item{period}{Character; one of \code{"year"}, \code{"semester"}, \code{"quarter"}, or
\code{"month"}.  Passed to \code{\link[base:seq.Date]{base::seq.Date()}} as the \code{by} argument and used to
match historical sub-periods.}

\item{length}{Integer.  Number of future periods to generate.}

\item{round_dates}{Character; one of \code{"ceiling"} (default), \code{"floor"}, or
\code{"none"}.  When not \code{"none"}, each generated date is rounded to the
boundary of \code{round_unit}.}

\item{round_unit}{Character scalar passed to \code{\link[lubridate:round_date]{lubridate::ceiling_date()}} or
\code{\link[lubridate:round_date]{lubridate::floor_date()}} when \code{round_dates != "none"} (e.g. \code{"month"}).}

\item{fillyears}{Integer.  Number of most-recent historical years to use when
computing rolling means.  Ignored when \code{minyear} is supplied.  Default \code{3}.}

\item{minyear}{Integer or \code{NULL}.  Earliest year to include in the historical
window.  If \code{NULL}, derived as \code{maxyear - fillyears}.}

\item{maxyear}{Integer or \code{NULL}.  Latest year to include in the historical
window.  If \code{NULL}, derived from \code{max(year(data$date))}.}

\item{present_vars}{Character vector of predictor column names, \strong{or}
\code{"detect"} to auto-select all numeric columns that do not contain \code{"_l"}
in their name (i.e., exclude pre-existing lag columns).}

\item{response_var}{Character scalar.  Name of the response/outcome column.
This column is excluded from predictor filling and set to \code{NA_real_} in
the returned rows.}
}
\value{
A tibble of future-dated rows (from \code{startdate} onwards) whose
columns match those of \code{data}.  Predictor columns are filled with rolling
historical means; \code{response_var} is \code{NA_real_}.
}
\description{
Appends future-dated rows to a time-series data frame by filling predictor
variables with the rolling mean of recent historical observations that share
the same sub-period (month, quarter, semester, or year).  The response
variable column is set to \code{NA} in all new rows.
}
\details{
The function builds a running \code{fill_data} table.  For each future date, it
finds the most recent 3 historical observations sharing the same sub-period,
averages them, and appends a new row.  Lag columns (suffix \verb{_l<n>}) are then
computed over the combined table and the function returns only the
future-dated rows, selecting the same columns as \code{data}.

If \code{oni_anom} is detected among the predictor columns, an \code{oni_phenom}
classification column (\code{"La Niña"}, \code{"El Niño"}, \code{"Normal"}) is appended
automatically.
}
\examples{
\dontrun{
future_rows <- extend_time_series(
  data         = historical_df,
  startdate    = "2025-01-01",
  period       = "quarter",
  length       = 4,
  round_dates  = "ceiling",
  round_unit   = "month",
  present_vars = "detect",
  response_var = "riceProd"
)
}

}
