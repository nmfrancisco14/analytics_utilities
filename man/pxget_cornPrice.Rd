% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cornPrice.R
\name{pxget_cornPrice}
\alias{pxget_cornPrice}
\title{Fetch Farmgate Corn Price Data from the PSA PXWeb API}
\usage{
pxget_cornPrice()
}
\value{
A tibble with at least the following columns:
\describe{
\item{\code{Geolocation}}{character — geolocation name (dots removed).}
\item{\code{Year}}{character — reported year.}
\item{\code{Period}}{character — reporting month name (e.g. \code{"January"}).}
\item{\code{cornPrice}}{numeric — reported farmgate corn price (PhP/kg).}
\item{\code{date}}{Date — last day of the reporting month.}
}
}
\description{
Downloads and tidies farmgate corn price (PhP/kg) data from the Philippine
Statistics Authority (PSA) OpenStat PXWeb endpoint, combining the old and
new price series, and returns a tibble with a computed period-end date column.
}
\details{
The function queries two PSA PXWeb endpoints (old series up to 2009; new
series from 2010 onwards) for yellow-matured corn prices across all
geolocations and reporting months; combines them via a full join; strips
dots from \code{Geolocation}; renames the value column to \code{cornPrice}; and
derives a \code{date} column representing the last calendar day of each
reporting month.

The old series covers 1979–2009; the new series covers 2010 onwards.  Rows
from the old series with \code{Year >= 2010} are dropped before joining to avoid
duplication.  The \code{date} value is the last calendar day of the month named
in \code{Period}.
}
\examples{
\dontrun{
df <- pxget_cornPrice()
head(df)
}

}
\references{
Philippine Statistics Authority. \emph{Farmgate Prices of Palay and Corn
(Old Series, 1979–2009).} PSA OpenStat PXWeb API.
\url{https://openstat.psa.gov.ph/PXWeb/api/v1/en/DB/2M/FG/0032M4AFP01.px}

Philippine Statistics Authority. \emph{Farmgate Prices of Palay and Corn
(New Series, 2010–present).} PSA OpenStat PXWeb API.
\url{https://openstat.psa.gov.ph/PXWeb/api/v1/en/DB/2M/NFG/0032M4AFN01.px}
}
