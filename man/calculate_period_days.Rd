% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_period_days.R
\name{calculate_period_days}
\alias{calculate_period_days}
\title{Compute the Number of Days in a Reporting Period}
\usage{
calculate_period_days(date_val, period_type_val)
}
\arguments{
\item{date_val}{A scalar that can be coerced to \code{Date} via \code{\link[base:as.Date]{base::as.Date()}}.
The year and month components of this value are used to identify the
enclosing period.}

\item{period_type_val}{Character scalar; one of \code{"Annual"}, \code{"Semestral"},
\code{"Quarterly"}, or \code{"Monthly"}. Case-sensitive.}
}
\value{
An integer scalar: the number of calendar days in the specified
reporting period.  If \code{period_type_val} does not match any of the four
recognised values, the function returns \code{365} as a fallback.
}
\description{
Given a date and a reporting period type, returns the total number of calendar
days that fall within that period (month, quarter, semester, or year).
}
\details{
\itemize{
\item \strong{Annual} — the full calendar year of \code{date_val}, accounting for leap years
(366 days) and common years (365 days).
\item \strong{Semestral} — the first semester runs 1 January – 30 June (181 or 182 days);
the second semester runs 1 July – 31 December (184 days).
\item \strong{Quarterly} — the three-month calendar quarter containing \code{date_val}
(Q1: Jan–Mar, Q2: Apr–Jun, Q3: Jul–Sep, Q4: Oct–Dec).
\item \strong{Monthly} — the calendar month containing \code{date_val}.
}
}
\examples{
# Leap-year annual
calculate_period_days("2024-07-15", "Annual")   # 366

# Common-year annual
calculate_period_days("2023-01-01", "Annual")   # 365

# Q1 of a non-leap year
calculate_period_days("2023-02-28", "Quarterly") # 90

# Month of February in a leap year
calculate_period_days("2024-02-10", "Monthly")   # 29

}
