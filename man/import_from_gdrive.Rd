% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_from_gdrive.R
\name{import_from_gdrive}
\alias{import_from_gdrive}
\title{Import a File from Google Drive}
\usage{
import_from_gdrive(file_link, authorize = TRUE, email = NULL, ...)
}
\arguments{
\item{file_link}{Character scalar.  A Google Drive share link or file ID
accepted by \code{\link[googledrive:drive_id]{googledrive::as_id()}}.}

\item{authorize}{Logical.  If \code{TRUE} (default), authenticate via
\code{\link[googledrive:drive_auth]{googledrive::drive_auth()}}.  Set to \code{FALSE} to use a previously cached
token or public files via \code{\link[googledrive:drive_deauth]{googledrive::drive_deauth()}}.}

\item{email}{Character scalar or \code{NULL}.  Google account email passed to
\code{\link[googledrive:drive_auth]{googledrive::drive_auth()}}.  Ignored when \code{authorize = FALSE}.}

\item{...}{Additional arguments forwarded to the underlying reader function
(\code{\link[readr:read_delim]{readr::read_csv()}}, \code{\link[openxlsx:read.xlsx]{openxlsx::read.xlsx()}}, or \code{\link[base:readRDS]{base::readRDS()}}).}
}
\value{
The parsed file contents:
\itemize{
\item \code{.csv} → a tibble (via \code{\link[readr:read_delim]{readr::read_csv()}})
\item \code{.xlsx} → a data frame (via \code{\link[openxlsx:read.xlsx]{openxlsx::read.xlsx()}})
\item \code{.rds} → the object stored in the RDS file
}
}
\description{
Downloads a file from Google Drive (by share link or file ID) to a temporary
path and reads it into R.  Supports CSV, Excel (\code{.xlsx}), and RDS formats.
}
\details{
The file is streamed to a temporary file (cleaned up on function exit via
\code{\link[base:on.exit]{base::on.exit()}}).  File type is determined from the Drive file's name
extension.  Unsupported extensions raise an error.
}
\examples{
\dontrun{
df <- import_from_gdrive(
  file_link = "https://drive.google.com/file/d/FILEID/view?usp=sharing",
  email     = "you@example.com"
)
}

}
