% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/riceStocks.R
\name{pxget_riceStocks}
\alias{pxget_riceStocks}
\title{Fetch Rice Stocks Inventory from the PSA PXWeb API}
\usage{
pxget_riceStocks()
}
\value{
A tibble with at least the following columns:
\describe{
\item{\code{Sector}}{character — type of stock (e.g. Household, Commercial,
Government/NFA, Total).}
\item{\code{Year}}{character — reported year.}
\item{\code{Month}}{character — reported month name (e.g. \code{"January"}).}
\item{\code{riceStocks}}{numeric — rice stocks at end of month (metric tons).}
\item{\code{date}}{Date — last calendar day of the reported month.}
}
}
\description{
Downloads and tidies monthly rice stocks inventory data from the Philippine
Statistics Authority (PSA) OpenStat PXWeb endpoint, and returns a tibble
with a computed month-end date column.
}
\details{
The function queries all sectors (0–3), a wide range of years (1980–2026),
and all months; uses \code{pxweb_advanced_get()} with metadata-based elimination
to handle the API's response structure; renames the value column to
\code{riceStocks}; and derives a \code{date} column for the last day of each reported
month.

The function calls \code{pxweb::pxweb_advanced_get()} with an intermediate
metadata object to set \code{elimination = TRUE} on the Year variable, which is
required by this particular PSA endpoint.  The \code{date} column is derived by
matching \code{Month} against \link[base:Constants]{base::month.name} and computing the month-end date
via \code{\link[lubridate:round_date]{lubridate::ceiling_date()}}.
}
\examples{
\dontrun{
df <- pxget_riceStocks()
head(df)
}

}
\references{
Philippine Statistics Authority. \emph{Rice and Corn: Monthly Total Stocks
Inventory by Sector.} PSA OpenStat PXWeb API.
\url{https://openstat.psa.gov.ph:443/PXWeb/api/v1/en/DB/2E/CS/0032E4ECNV0.px}
}
