% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psa_psgc_pop2024.R
\name{get_psgc_pop2024}
\alias{get_psgc_pop2024}
\title{Build Tidy PSGC 2024 Geographic Tables from a PSA Excel File}
\usage{
get_psgc_pop2024(file_path)
}
\arguments{
\item{file_path}{Character scalar. Path to the PSGC Excel workbook (the
sheet named \code{"PSGC"} is used).  The file should be the official PSA
publication dated 30 September 2025 or later.}
}
\value{
A named list with four tibbles:
\describe{
\item{\code{regions}}{Region-level records with columns \code{reg_psgc},
\code{reg_ccode}, \code{region}, \code{x2024_population}.}
\item{\code{provinces}}{Province/HUC records joined to their parent region.}
\item{\code{municities}}{Municipality/city/sub-municipality records joined
to their parent region and province.}
\item{\code{barangays}}{Barangay records joined to their parent region,
province, and municipality/city.}
}
}
\description{
Reads the official Philippine Standard Geographic Code (PSGC) Excel workbook
published by the Philippine Statistics Authority (PSA) and returns a named
list of four tidy tibbles: regions, provinces/HUCs, municipalities/cities,
and barangays — each enriched with parent-level codes and names.
}
\details{
The function uses \code{\link[openxlsx:read.xlsx]{openxlsx::read.xlsx()}} to read the workbook and
\code{\link[janitor:clean_names]{janitor::clean_names()}} to snake-case column names.  Parent codes are
derived by slicing the 10-digit PSGC code:
\itemize{
\item Region code — first 2 digits padded to 10 digits with zeros.
\item Province code — first 5 digits padded to 10 digits with zeros.
\item Municipality code — first 7 digits padded to 10 digits with zeros.
}

Highly Urbanised Cities (HUCs) are treated as province-equivalent units and
are included in the \code{provinces} table.
}
\examples{
\dontrun{
psgc <- get_psgc_pop2024("path/to/psgc_pop2024.xlsx")
head(psgc$regions)
head(psgc$provinces)
}

}
\references{
Philippine Statistics Authority. \emph{Philippine Standard Geographic Code
(PSGC), Publication Date: 30 September 2025.}
\url{https://psa.gov.ph/classification/psgc}
}
