% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prismPAY.R
\name{sbget_payPrism}
\alias{sbget_payPrism}
\title{Retrieve PRiSM Production, Area, and Yield Records from a Supabase REST API}
\usage{
sbget_payPrism(
  supabase_url,
  supabase_key,
  table = "pay_prism",
  location_field = "location",
  location_value = "Philippines",
  period_col = "PERIOD",
  year_col = "YEAR",
  parse_month_end = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{supabase_url}{Character. Base Supabase project URL (e.g.
\code{"https://<project-ref>.supabase.co"}).  Do \strong{not} include the
\verb{/rest/v1} suffix.}

\item{supabase_key}{Character. Supabase \code{anon} or \code{service_role} API key
used for the \code{apikey} and \code{Authorization: Bearer} headers.}

\item{table}{Character. REST table to query. Default: \code{"pay_prism"}.}

\item{location_field}{Character. Column name to filter by location.
Default: \code{"location"}.}

\item{location_value}{Character. Value to match in \code{location_field}.
Default: \code{"Philippines"}.}

\item{period_col}{Character. Column containing month names (e.g.
\code{"January"}). Default: \code{"PERIOD"}.}

\item{year_col}{Character. Column containing the integer year. Default:
\code{"YEAR"}.}

\item{parse_month_end}{Logical. If \code{TRUE} (default), append a \code{date} column
with the last day of the month derived from \code{year_col} and \code{period_col}.}

\item{verbose}{Logical. If \code{TRUE}, print the request URL to the console.
Default: \code{FALSE}.}
}
\value{
A tibble of the parsed JSON response, filtered to rows where
\code{period_col} is one of \link[base:Constants]{base::month.name}.  If \code{parse_month_end = TRUE}
and the result is non-empty, a \code{date} column of class \code{Date} is appended.
}
\description{
Queries a Supabase REST endpoint for PRiSM Production, Area, and Yield
(PAY) data, filters rows by a location value and by valid English month
names, and optionally appends a month-end \code{date} column.
}
\details{
The function constructs a PostgREST equality filter (\verb{?<field>=eq.<value>})
and sets the required Supabase authentication headers.  An HTTP error will
cause \code{\link[httr:stop_for_status]{httr::stop_for_status()}} to stop with an informative message.
}
\examples{
\dontrun{
df <- sbget_payPrism(
  supabase_url   = "https://xyzabc.supabase.co",
  supabase_key   = Sys.getenv("SUPABASE_KEY"),
  location_value = "Philippines"
)
head(df)
}

}
