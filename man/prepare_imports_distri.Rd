% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_imports_distri.R
\name{prepare_imports_distri}
\alias{prepare_imports_distri}
\title{Prepare Import Distribution Data for a Reporting Period}
\usage{
prepare_imports_distri(csv_file, date)
}
\arguments{
\item{csv_file}{Character scalar. Path to a CSV file containing import
distribution records.  Must include at least the columns \code{port_name},
\code{def_prov}, \code{def_prov_psgc}, \code{alloc_vol_mt}, \code{pre_import_vol_mt},
\code{post_import_vol_mt}, and \code{iteration}.}

\item{date}{A scalar coercible to \code{Date} via \code{\link[lubridate:ymd]{lubridate::ymd()}}.  Used to
populate the \code{as_of_date} column in the output (e.g. \code{"2026-06-30"}).}
}
\value{
A tibble with the same column set as the existing
\code{importsDistributionByPortQ1} API table (excluding \code{id}, \code{created_at}, and
\code{updated_at}).  Each row represents a port–province distribution record
enriched with:
\describe{
\item{\code{fid}}{Integer row sequence.}
\item{\code{as_of_date}}{Date derived from \code{date}.}
\item{\code{period}}{Fixed string \code{"Quarter 2"}.}
\item{\code{year}}{Fixed integer \code{2026}.}
\item{\code{port_psgc}}{PSGC code of the port, joined from \code{riceImportsByStation}.}
\item{\code{def_prov_psgc}}{Province PSGC code, joined from the SUA combined data.}
}
}
\description{
Reads a CSV file of port-level import distribution records, enriches it with
PSGC province codes derived from the API, aligns column names to the existing
\code{importsDistributionByPortQ1} schema, and prints a reconciliation summary
comparing total allocated volumes against actual import data.
}
\details{
The function performs the following steps:
\enumerate{
\item Fetches existing distribution schema and import-by-station data from the
DAC Lake API via \code{daclakeapi::get_api_data()}.
\item Builds a port → PSGC lookup table, overriding known mismatches for
SAN FERNANDO, MICP, and POM.
\item Fetches SUA combined data (FIES, 2025) to obtain province PSGC codes,
and derives an island-group flag (\code{LZN}, \code{VIS}, \code{MIN}).
\item Reads \code{csv_file}, standardises \code{port_name} to upper-case and \code{def_prov}
to lower-case (with a manual fix for \code{"Samar"}), then joins the port and
province PSGC lookups.
\item Prints a reconciliation table of total imports, total allocated volume,
pre-distribution volume (iteration = 0), and post-distribution volume
(max iteration).
}
}
\examples{
\dontrun{
result <- prepare_imports_distri(
  csv_file = "data/import_distri_q2_2026.csv",
  date     = "2026-06-30"
)
}

}
